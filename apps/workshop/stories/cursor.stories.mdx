import { Cursor } from 'ui'
import { Canvas, Meta, Story } from '@storybook/blocks'

<Meta
  title="Components/Cursor"
  parameters={{
    layout: 'centered',
  }}
  component={Cursor}
/>

export const Template = (args) => <Cursor {...args} />

# Cursor

Cursor component is used to display a cursor on the screen.

<Canvas>
  <Story
    name="Default"
    args={{
      color: 'red',
      label: 'Cursor',
      x: 0,
      y: 0,
      size: 32,
      clicked: false,
      interpolate: false,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
